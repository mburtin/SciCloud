{
  "extends": "./tsconfig.base.json",
  "compilerOptions": {
    "composite": true,                          // Enable project references
    "incremental": true                         // Incremental compilation for better performance
  },
  "references": [
    { "path": "./apps/web" }
  ]
}
